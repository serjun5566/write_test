
	<?PHP require_once 'topbar.php';?>
<?PHP
	require_once 'database.php';
	$conn= new mysqli($hn,$un,$pw,$db);
	$query="SELECT * FROM `problems` where `unit`=".$_GET['unit'];
	$result=$conn->query($query);
	$ROW=$result->num_rows;
	
	
	if($_GET['unit']==1)	
		$unit_chinese="unit一";
	
	else if($_GET['unit']==2)
		$unit_chinese="unit二";
	
	else if($_GET['unit']==3)
		$unit_chinese="unit三";
	
	else if($_GET['unit']==4)
		$unit_chinese="unit四";

		for($i=0;$i<ceil($ROW/50);$i++)
		{
			if($i<ceil($ROW/50)-1)
				echo"<a href='http://localhost/write_test/problems.php?unit=".$_GET['unit']."&&area=".$i."'>".$unit_chinese.($i*50+1)."到".(($i+1)*50)."</a><br>";
			else
				echo"<a href='http://localhost/write_test/problems.php?unit=".$_GET['unit']."&&area=".$i."'>".$unit_chinese.($i*50+1)."到".$ROW."</a><br>";
		}
$conn->close();
?>